extends@: default

form:
  fields:
    tabs:
      fields:
        content:
          fields:
            taxonomies:
              type: section
              title: PLUGIN_ADMIN.TAXONOMIES
              underline: true

              fields:
                header.taxonomy.type:
                  type: select
                  label: type
                  options:
                    'post': TYPES.POST
                    'short': TYPES.SHORT
                    'notice': TYPES.NOTICE

        options:
          fields:
            taxonomies:
              unset@: true
