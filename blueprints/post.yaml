extends@: default

form:
  fields:
    tabs:
      fields:
        content:
          fields:
            taxonomies:
              type: section
              title: PLUGIN_ADMIN.TAXONOMIES
              underline: true

              fields:
                header.taxonomy.type:
                  type: select
                  label: type
                  options:
                    'billet': 'billet'
                    'brève': 'brève'
                    'notification': 'notification'

        options:
          fields:
            taxonomies:
              unset@: true
